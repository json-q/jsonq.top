name: Deploy-nest

on:
  push:
    branches:
      - server

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # - name: Set up Node.js
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: '16'

      - name: Copy scripts to remote server via SCP
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: 'scripts/setup.sh'
          target: '/software/jsonq.top/server/scripts'

      - name: Run setup script on remote server via SSH
        uses: garygrossgarten/github-action-ssh@release
        with:
          command: |
            cd /software/jsonq.top/server
            chmod +x ./scripts/setup.sh
            bash ./scripts/setup.sh
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          privateKey: ${{ secrets.SSH_PRIVATE_KEY}}

      # - name: Run setup script
      #   run: |
      #     cd /software/jsonq.top/server && git pull origin server && npm install && bash ./scripts/setup.sh
